Using python 3.12 as it's safer to run with the libraries we intend to use.

created venv, to run, do "source .venv/bin/activate"

perhaps at some point do a mermaid chart? 

ad_hoc_drifter generates drifted data - do at a later stage! 

Info about the dataset:
Important to note that we're dealing with cross-sectional data (tvärsnittsdata). I.e. we don't have any dates in the data as this is a "snapshot" in time.

Files and what their purpose is: 
- ingest_data.py is an ad hoc script only to create a database from the CSV.
